Client-server multi-threaded application, calculating *sin*, *sqrt* and *pow* operations in different threads.

Each client generates the list of its calculations, up to 100 items.
After that, the results get checked consequently.

There can currently be messages of pow results' mismatches.